export enum TaskStatus {
  TODO = 'todo',
  IN_PROGRESS = 'in-progress',
  DONE = 'done',
}

export enum TaskFilterStatus {
  ALL = 'all',
  TODO = 'todo',
  IN_PROGRESS = 'in-progress',
  DONE = 'done',
}
